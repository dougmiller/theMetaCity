body {
    margin: 0;
    display: grid;
    height: 100vh;
    grid-template-columns: 1fr;
    grid-template-rows: 120px 1fr;
    grid-row-gap: 10px;
    grid-template-areas:
        "navigation"
        "articles"
        "links";
}

h1 {
    margin: 0;
    font-size: 1.1em;
    font-family: var(--font-heading);

    a {
        color: var(--colour-alight);
        text-decoration: none;
    }

    a:hover {
        color: var(--colour-work);
    }

    a:visited {
        color: var(--colour-known);
    }
}

h2 {
    font-size: 0.9em;
    margin: 0.5em 0 0 0;
    font-family: var(--font-lead)
}

h3 {
    font-size: 0.8em;
    margin: 0.5em 0 0 0;
    font-family: var(--font-lead);

    a {
        color: var(--colour-alight);
        text-decoration: none;
    }

    a:hover {
        color: var(--colour-work);
    }

    a:visited {
        color: var(--colour-known);
    }
}

nav, article, footer {
    background-color: var(--colour-mist);
    border: 1px solid var(--colour-unforgiving);
}

nav {
    grid-area: navigation;

    display: grid;
    grid-template-columns: 120px 1fr;
    grid-column-gap: 1em;
    grid-template-rows: repeat(3, 1fr);
    grid-template-areas:
        "imglogo name"
        "imglogo blog"
        "imglogo workshop";

    align-items: center;

    img {
        grid-area: imglogo;
        width: 120px;
    }

    a:nth-of-type(1) {grid-area: name; margin-top: 1em;}
    a:nth-of-type(2) {grid-area: blog;}
    a:nth-of-type(3) {grid-area: workshop; margin-bottom: 1em;}
}

main {
    grid-area: articles;

    article {
        padding: 1em;
        margin: 0 0 1em 0;
    }

    article.blog {border: 1px solid var(--colour-work);}
    article.workshop {border: 1px solid var(--colour-explore);}

    article:last-child {
        margin: 0;
    }
}

footer {
    padding: 1em;
    text-align: center;

    grid-area: links;
    align-self: end;

    display: flex;
    justify-content: space-between;

    h3 {
        a {
            color: var(--colour-alight);
            text-decoration: none;
        }
    }
}