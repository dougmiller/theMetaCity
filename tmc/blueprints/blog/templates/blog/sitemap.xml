<?xml version="1.0" encoding="UTF-8"?>
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">
    <url>
        <loc>{{ url_for('blog.home', _external=True) }}</loc>
        <lastmod>{{ articles[0].sitemap_lastmod }}</lastmod>
        <changefreq>monthly</changefreq>
    </url>

    {% for article in articles %}
        <url>
            <loc>{{ url_for('blog.title', url=article.url, _external=True) }}</loc>
            <lastmod>{{ article.sitemap_lastmod }}</lastmod>
            <changefreq>never</changefreq>
        </url>
    {% endfor %}

    <url>
        <loc>{{ url_for('blog.tags', _external=True) }}</loc>
        <changefreq>monthly</changefreq>
    </url>

    {% for article_tag in article_tags %}
        <url>
            <loc>{{ url_for('blog.tags_tag', tag=article_tag.tag, _external=True) }}</loc>
            <changefreq>monthly</changefreq>
        </url>
    {% endfor %}
</urlset>